# gpu-instancing-demo
A lightweight GPU instancing demo improving framerate by X% over standard non-batched rendering. Features per-instance property overrides via MaterialPropertyBlock and vertex-based shader animation.
